#!/bin/sh
### BEGIN INIT INFO
# Provides:          liquidgui
# Required-Start:    $remote_fs $syslog
# Required-Stop:     $remote_fs $syslog
# Default-Start:     2 3 4 5
# Default-Stop:      0 1 6
# Short-Description: Run Liquidctl fan curve daemon
### END INIT INFO

case "$1" in
  start)
    /usr/local/bin/liquidgui --daemon &
    ;;
  stop)
    pkill -f "liquidgui --daemon" || true
    ;;
  restart|force-reload)
    pkill -f "liquidgui --daemon" || true
    /usr/local/bin/liquidgui --daemon &
    ;;
  *)
    echo "Usage: /etc/init.d/liquidgui {start|stop|restart}" >&2
    exit 3
    ;;
 esac
